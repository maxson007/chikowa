{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Ajouter un Membre{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-12 d-flex justify-content-center">
                <div class="col-8">
                    <h1 class="h2 text-orange mb-3">Ajouter un Membre</h1>
                    {{ include('chikowa/association/_add_membre.html.twig') }}
                </div>
            </div>
        </div>
        {# <a href="{{ path('chikowa_association_index') }}">back to list</a> #}
    </div>
{% endblock %}