{% extends 'base.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ association.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ association.libelle }}</td>
            </tr>
            <tr>
                <th>TypeEntite</th>
                <td>{{ association.typeEntite }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ association.localisation }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ association.pays }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ association.ville }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chikowa_association_index') }}">back to list</a>

    <a href="{{ path('chikowa_association_edit', {'id': association.id}) }}">edit</a>

    {{ include('chikowa/association/_delete_form.html.twig') }}
{% endblock %}
