{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inscription.id }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ inscription.dateCreation ? inscription.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chikowa_inscription_index') }}">back to list</a>

    <a href="{{ path('chikowa_inscription_edit', {'id': inscription.id}) }}">edit</a>

    {{ include('chikowa/inscription/_delete_form.html.twig') }}
{% endblock %}
