{% extends 'base.html.twig' %}

{% block title %}Tontine{% endblock %}

{% block body %}
    <h1>Tontine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tontine.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ tontine.libelle }}</td>
            </tr>
            <tr>
                <th>MontantParMembre</th>
                <td>{{ tontine.montantParMembre }}</td>
            </tr>
            <tr>
                <th>FrequencePaiement</th>
                <td>{{ tontine.frequencePaiement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chikowa_tontine_index') }}">back to list</a>

    <a href="{{ path('chikowa_tontine_edit', {'id': tontine.id}) }}">edit</a>

    {{ include('chikowa_tontine/_delete_form.html.twig') }}
{% endblock %}
