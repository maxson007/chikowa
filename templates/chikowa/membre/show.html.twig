{% extends 'base.html.twig' %}

{% block title %}Membre{% endblock %}

{% block body %}
    <h1>Membre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ membre.nom }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ membre.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chikowa_membre_index') }}">back to list</a>

    <a href="{{ path('chikowa_membre_edit', {'id': membre.id}) }}">edit</a>

    {{ include('chikowa_membre/_delete_form.html.twig') }}
{% endblock %}
